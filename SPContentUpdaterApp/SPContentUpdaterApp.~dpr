program SPContentUpdaterApp;



{%File '..\SPContentUpdater\SPItem.pas'}

uses
  Forms,
  MainUnit in 'MainUnit.pas' {MainForm},
  SPEditor_TLB in '..\SPContentUpdater\SPEditor_TLB.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TMainForm, MainForm);
  Application.Run;
end.
